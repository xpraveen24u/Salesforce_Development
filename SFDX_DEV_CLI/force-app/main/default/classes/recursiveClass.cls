public with sharing class recursiveClass {
 public static Boolean triggerEnd = true;

 /*public static void  stopTrigger(){
    if(recursiveClass.triggerEnd == false){
        recursiveClass.triggerEnd = true;
    }else{
        recursiveClass.triggerEnd = false;
    }
 }*/
    
}
